services:
    database:
      image: postgres:15.4
      ports:
        - 5432:5432
      environment:
        POSTGRES_PASSWORD: postgres
        POSTGRES_USER: postgres
        POSTGRES_DB: efimera
      volumes:
        - db_data:/data/db

volumes:
  db_data:
